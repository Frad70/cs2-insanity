# Iteration 6 — 2026-04-30 ~22:50

## Shipped
- **v0.8.0** — per-bot AimProfile.
  - Extended `BotPersona` with 10 aim fields (snap/bias/refresh/reaction/overshoot×2/noise/micro-adjust/spray-ctrl/flick-strength).
  - Added `BotArchetype` enum (Generic, Entry, Lurker, AwperPassive, AwperAggro, Support, Anchor, IGL, BaitOMatic, HeadshotOnly).
  - `ApplySkillToAim()` rolls aim params from persona.Skill (0.40..1.80).
  - `ApplyArchetype()` rolls archetype with skill-weighted distribution.
  - `AimController` now reads per-slot `AimProfile` via `SetProfile(slot, profile)`.
  - Reaction-time gate: bot doesn't move crosshair until `ReactionTimeSec` after spotting target.
  - Overshoot baked into goal (one-shot) for eager bots.
  - Per-tick tracking noise for low-skill only.
  - Micro-adjust mid-engage for high-skill (S>1.20).
  - Flick strength multiplier on big aim deltas (>8°).
  - Wired in `OnPlayerSpawn` and the lazy-create path in `ScheduleBotChat`.
  - Build: 0 errors, 2 stale warnings.
  - Deployed to live server. rcon reload command timed out (server is up — known
    Steam pressure-vessel issue) but DLL is on disk; CSSharp will pick on next
    plugin reload triggered in-game.
  - Commit `c7d27d9`, pushed to `origin/main`.

## In flight (parallel agents)
- **v0.9.0** — zone-aware callouts, dedup, echo/rebuke, world-event triggers.
  Agent `a9acc2abfb4a36dad`.
- **v0.10.0** — movement realism (walk pulses, crouch-on-awp-spot, pre-aim
  heuristic, entry rush). Strict no-velocity-write rule passed in.
  Agent `ac01babb416408961`.

## Reverse-engineering branch (E)
Still blocked. Findings.md updated honestly: candidate NOP patches at
`ja 0xb46330` disconfirmed (that's the friend/enemy counter, not threat list).
Player-iter vcalls are constants/stubs on CCSPlayerPawn — non-trivial on
CCSPlayerController but no obvious team-cmp shape. Pivot for next iter:
disasm UpdateReactionQueue (fcn.00b478f0) — only caller of FindMostDangerous.

## Notes / lessons
- User's pushback on patching prematurely was correct. Both candidate patches
  would have broken counter logic without changing threat-list membership.
- The `r2 aaa` background pass is still running but project save dir empty.
- I committed the journal directory. User said matom можно — keeping it clean
  for now since the file is in the repo.

## Next iteration
Email update at v0.10 ship. Otherwise continue building. If parallel agents
finish or block, pick up the next branch.
